@using EPAM.SUMMER.FORUM.ZHELDAK.ViewModels.ComentsModels
@model CommentForAdminModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete comment?</h3>
<div class="row">
    <div class="col-md-8 col-lg-offset-2">@Model.Comment</div>
</div>
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m=>m.Id)

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back","Admin", "Admin")
        </div>
    }

